'use strict';

/**
 * site-parameter controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::site-parameter.site-parameter');
